name: my first workflow

on: push


jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: List and Read files
        run: 
          echo "My first Github action job"
          ls -lstr
          cat src/main/java/com/gomo/SpringBoot/with/GitHub/actions/HelloWorld.java

      # - name: Generate ASCII Artwork
      #   run: cowsay -f dragon "Run for cover , I am a DRAGON...RAWR" >> dragon.txt
        

      # - name: List files
      #   run: ls

      # - name: Read HelloWorld.java
      #   run: cat src/main/java/com/gomo/SpringBoot/with/GitHub/actions/HelloWorld.java

